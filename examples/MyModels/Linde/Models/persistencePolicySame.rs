
%3) Markov-switching shock persistence and policy coefficients with the same process;
@#include "fwz_est_pers.rs"
% persistence is controlled by markov chain vol.
@#include "switching_persistence.rs"
% Now we also add the policy parameters letting them be controlled by the same process
@#include "switching_policy_parameters_persistenceChain.rs"

